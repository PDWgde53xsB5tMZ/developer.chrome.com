/**
 * This function takes in a `data` object as its only argument and returns an object with three properties: `title`, `description`, and `authors`.
 *
 * The `title` property is generated by concatenating the string "Preview: " with the value of the `title` property of the `fm` property of the `data` object. If the `fm` property or the `title` property does not exist, then `title` will be `undefined`.
 *
 * The `description` property is set to the value of the `description` property of the `fm` property of the `data` object. If the `fm` property or the `description` property does not exist, then `description` will be `undefined`.
 *
 * The `authors` property is set to the value of the `authors` property of the `fm` property of the `data` object. If the `fm` property or the `authors` property does not exist, then `authors` will be `undefined`.
 *
 * @param {Object} data - The input data object
 * @return {Object} An object with three properties: `title`, `description`, and `authors`
 */
module.exports = data => {
  return {
    title: `Preview: ${data.fm?.title}`,
    description: data.fm?.description,
    authors: data.fm?.authors,
  };
};
